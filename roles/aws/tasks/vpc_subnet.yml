- name: Create subnet for Instance
  ec2_vpc_subnet:
    state: present
    vpc_id: "{{ vpc_id }}"
    region: "{{ region }}"
    az: "{{ az1 }}"
    cidr: "10.0.0.128/25"
    tags:
      Name: demo
  register: myvpcsubnet
  tags: demo

- debug: msg="{{ myvpcsubnet.subnet.id }}"
  tags: demo
